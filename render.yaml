services:
  - type: web
    name: flask-youtube-transcript
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: PORT
        value: 5000
    healthCheckPath: / 